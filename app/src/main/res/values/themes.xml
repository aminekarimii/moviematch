<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Allows us to override night specific attributes in the values-night folder. -->
    <style name="Theme.MovieMatcher" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MovieMatcher.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.MovieMatcher</item>
    </style>
</resources>